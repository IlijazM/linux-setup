#!/bin/bash

echo "Installing nginx..."
if gum spin --title "apt update" -- apt update \
   && gum spin --title "apt install certbot nginx -y" -- apt install certbot nginx -y;
then
  echo "✅ nginx installed."
else
  echo "❌ Failed to install nginx."
  exit 1
fi
